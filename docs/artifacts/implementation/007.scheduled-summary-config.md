# 007. scheduled-summary (config 부분) — 구현 로그

- **날짜**: 2026-04-18
- **작업 개요**: `SummaryConfig` 타입과 summary 관련 환경변수 파싱을 config.go에 추가

---

## 작업 항목 진행 현황

| # | 항목 | 상태 | 비고 |
|---|------|------|------|
| 1 | `SummaryConfig` 타입 정의 (Schedule, WebhookURL, StaleDays) | 완료 | yaml 태그 포함 |
| 2 | Config 구조체에 `Summary *SummaryConfig` 필드 추가 | 완료 | State 필드 뒤 배치 |
| 3 | `SUMMARY_SCHEDULE` / `SUMMARY_WEBHOOK_URL` / `SUMMARY_STALE_DAYS` 환경변수 파싱 | 완료 | STATE_PATH 블록 뒤 배치 |
| 4 | `SUMMARY_STALE_DAYS` 숫자 파싱 에러 반환 | 완료 | `strconv.Atoi` 에러 래핑 |
| 5 | config_test.go에 summary 환경변수 파싱 테스트 | 완료 | `summary env variables` |
| 6 | config_test.go에 `SUMMARY_STALE_DAYS` 파싱 에러 테스트 | 완료 | `SUMMARY_STALE_DAYS parsing error` |
| 7 | config.yaml.example에 summary 블록 추가 | 완료 | state 블록 뒤 |

## 설계 대비 변경

없음. Design 문서 섹션 2, 6.1을 그대로 따름.

## 기술 부채

없음.

## 생성/수정 파일

- `config.go` — `SummaryConfig` 타입, Config.Summary 필드, summary 환경변수 파싱 3개 추가
- `config_test.go` — summary 환경변수 테스트 2개 추가
- `config.yaml.example` — summary 블록 예시 추가

## 미검증 항목

- `go build` / `go test` 실행 환경 부재로 로컬 컴파일 검증 불가. 코드는 기존 동일 패턴(`NOTIFICATION_WEBHOOK_URL`, `STATE_PATH`)을 그대로 따랐으며 문법 오류 가능성은 낮음.
